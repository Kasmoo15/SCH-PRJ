Display "### Calculator ###"
Ask the user to choose between 1:addition 2:subtract 3:multiply 4:divide
Display to the user "Enter the first number"
Store the user input into "num1"
Display to the user "Enter the second number"
Store the user input int "num2"
Display to the user "the result is " + num 1 + num2
Display to the user "Other calculation"
if user input is "yes"
    done == false
else
    done == yes
Process finished